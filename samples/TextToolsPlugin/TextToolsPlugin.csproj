<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>TextToolsPlugin</AssemblyName>
  </PropertyGroup>
  <ItemGroup>
    <!-- The host provides this assembly at runtime, so don't copy it next to the plugin. -->
    <ProjectReference Include="../../src/BlankSlate.PluginApi/BlankSlate.PluginApi.csproj" Private="false" />
  </ItemGroup>
</Project>
